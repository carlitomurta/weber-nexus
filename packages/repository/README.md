# @weber-nexus/repository

Backend repository package for Nexus.

This package owns data access and depends on `@weber-nexus/database`, so apps can use repositories without importing database schemas or clients directly.
